# Prompt Decorator

Mutates the final prompt text during `prompt.post_build`.

## Scripts

- `npm test` compiles the project and runs the local template test.
- `npm run build` emits `dist/` for use as the extension subprocess entrypoint.
