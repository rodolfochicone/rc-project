# Learnings

## verification
- [0.5] ao editar skill/template/manifest neste repo (markdown+scripts, sem build) → grep por links/refs pendentes e validar JSON/frontmatter antes de dar por concluído  (evidence: ciclo edit→grep/validar repetido ~10× em 2026-07-12; seen 1)
