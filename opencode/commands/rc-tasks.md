---
description: rc — break PRD+TechSpec into tasks (Qwen3.7 Max, high)
agent: rc-tasks
---
Break the approved PRD + TechSpec into executable task files for: $ARGUMENTS

Use the `rc-create-tasks` skill. Read `_prd.md` and `_techspec.md` for the slug first.
