---
description: rc — branch, commit, PR (DeepSeek V4 Flash, low)
agent: rc-git
---

Move the current changes onto a feature branch and open a PR: $ARGUMENTS

Use the `rc-git` skill. Confirm each outward-facing step and the PR target. No AI co-author/attribution.

Once the PR is open, run the `rc-instincts` skill to distill this session's recurring patterns into `.rc/instincts/INSTINCTS.md` (no-op when nothing durable is worth capturing). Skip it if the ship step stopped without opening a PR.
