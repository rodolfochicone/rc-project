---
description: rc — implement one hard task (GLM-5.2, high)
agent: rc-exec
---

Implement the following rc task end to end, with verification: $ARGUMENTS

Use the `rc-execute-task` skill. Run tests/lint and show output before claiming done; verify with `rc-final-verify`.
